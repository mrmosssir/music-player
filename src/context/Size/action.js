import { SET_PREVIEWLIST_COLS, SET_MAIN_REF } from './constant';

export const setPreviewListCols = data => ({ type: SET_PREVIEWLIST_COLS, data });
export const setMainRef = data => ({ type: SET_MAIN_REF, data });